"use client";

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function BlogTopicClusterManager() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Blog Topic Cluster Manager</CardTitle>
      </CardHeader>
      <CardContent className="text-sm text-muted-foreground">
        Organize blog posts into topic clusters for better internal linking and SEO. Placeholder
        component for your blog admin workflows.
      </CardContent>
    </Card>
  );
}


