"use client";

import { useEffect } from "react";

export default function HomepageScrollTracker() {
  useEffect(() => {
    // Placeholder tracker to preserve homepage import contract.
  }, []);

  return null;
}
