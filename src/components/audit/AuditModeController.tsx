"use client";

import { useEffect } from "react";

export default function AuditModeController() {
  useEffect(() => {
    // Lightweight stub to keep layout imports stable.
    // Future audit-mode behavior can be reintroduced here.
  }, []);

  return null;
}
